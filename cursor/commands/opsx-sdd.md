---
description: 'OpenSpec OPSX 入口 — 顯示指令路由表並引導選擇 explore/propose/apply/verify/archive。Use when: starting OPSX workflow, unsure which opsx command to use.'
---

載入 opsx-sdd skill 並輸出指令路由表。

#skill:opsx-sdd

當使用者輸入 `/opsx-sdd` 時，直接輸出 SKILL.md 中的**指令路由表**與**流程總覽**。
根據使用者的描述，推薦最適合的 `/opsx-*` 指令作為下一步。

開始。
